# Streamlit Secrets Configuration
# Rename this file to secrets.toml to use locally

# Set to true to run with mock data (no backend required)
DEMO_MODE = true

# API Configuration (ignored if DEMO_MODE is true)
API_BASE_URL = "http://localhost:8000"
